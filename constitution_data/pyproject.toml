[project]
name = "constitution-data"
version = "0.1.0"
description = ""
authors = [
    {name = "Kaustav Sarkar"}
]
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "transformers",
    "accelerate",
    "torch",
    "pymupdf",
    "tqdm",
    "regex",
    "datasets",
    "llama-stack (>=0.3.5,<0.4.0)",
    "llama-cpp-python (>=0.3.16,<0.4.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
